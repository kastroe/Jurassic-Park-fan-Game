---
description: Check environment variables for MCPs
---

Check that all required environment variables for our MCPs are set:

1. `GITHUB_TOKEN` — for github-mcp (check ~/.profile)
2. Any other MCP env vars

Report which are set, which are missing, and where to configure them.
