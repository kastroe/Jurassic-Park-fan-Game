---
description: Wraps built-in /compact — summarize and shrink session context
---

Runs the built-in /compact command to summarize and compress
the current session, freeing context space. This is a direct alias
for OpenCode's native /compact.
