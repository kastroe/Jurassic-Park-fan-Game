---
description: Show current model/provider configuration
---

Read the model config from @opencode.json.
Report what model is used for:
1. Main tasks (model)
2. Small/cheap tasks (small_model)
3. Any per-agent model overrides
4. Whether the configured models are optimal for the task at hand
