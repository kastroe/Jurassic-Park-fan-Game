---
name: state-management-patterns
description: Patterns for state management (Redux, Zustand, Signals, Context, etc.).
origin: Custom
---
State management: Keep state as local as possible. Use server state (React Query/SWR) for async data. Use client stores only for UI state (modals, sidebar). Prefer Zustand or Signals over Redux boilerplate. Activate when designing or reviewing state architecture.
