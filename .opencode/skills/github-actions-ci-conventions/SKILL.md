---
name: github-actions-ci-conventions
description: GitHub Actions workflow structure, caching, and CI patterns.
origin: Custom
---
GitHub Actions conventions: Use reusable workflows to avoid duplication. Cache dependencies between runs. Use matrix builds for multi-version testing. Keep jobs focused (lint → test → build → deploy). Activate when writing or reviewing CI workflows.
