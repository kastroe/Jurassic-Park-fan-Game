---
name: graphql-conventions
description: GraphQL schema design, resolver patterns, and query conventions.
origin: Custom
---
GraphQL conventions: Use codegen for types. Implement DataLoader for N+1 prevention. Keep resolvers thin (delegate to services). Use fragments on the client, not deeply nested queries. Prefer connections for pagination. Activate when writing or reviewing GraphQL code.
