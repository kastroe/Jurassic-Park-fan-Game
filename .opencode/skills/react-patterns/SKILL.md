---
name: react-patterns
description: React component patterns, hooks conventions, and rendering strategies.
origin: Custom
---
React patterns: Prefer functional components and hooks over classes. Keep state local, lift when shared. Use useCallback/useMemo only when profiling shows a need. Prefer derived state over useEffect for computed values. Server components by default (Next.js App Router). Activate when writing or reviewing React code.
